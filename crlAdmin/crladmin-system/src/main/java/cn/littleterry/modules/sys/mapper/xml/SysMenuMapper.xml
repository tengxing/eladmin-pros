<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.littleterry.modules.sys.mapper.SysMenuMapper">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="cn.littleterry.modules.sys.entity.SysMenu" id="sysMenuMap">
        <result property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="componentName" column="component_name"/>
        <result property="sort" column="sort"/>
        <result property="icon" column="icon"/>
        <result property="description" column="description"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <resultMap id="treeModelMap" type="cn.littleterry.modules.sys.entity.dto.TreeModel">

    </resultMap>

    <select id="findByUser" parameterType="java.lang.String"  resultMap="sysMenuMap">
	    SELECT
            m.*
        FROM
            sys_menu m
        JOIN sys_role_menu a ON a.menu_id = m.id
        JOIN sys_role b ON a.role_id = b.id
        JOIN sys_user_role c ON c.role_id = b.id
        JOIN sys_user d ON d.id = c.user_id
        WHERE
            d.username = #{username,jdbcType=VARCHAR}
	</select>

    <select id="findByRoleId" parameterType="java.lang.Long" resultMap="sysMenuMap">
        SELECT
            m.*
        FROM
            sys_menu m
        WHERE
            m.id IN (
                SELECT
                    menu_id
                FROM
                    sys_role_menu rm
                JOIN sys_role r ON rm.role_id = r.id
                WHERE
                    r.id= #{id,jdbcType=INTEGER}
            )
    </select>

    <select id="selectAll" resultMap="treeModelMap">
        select * from sys_menu;
    </select>
</mapper>