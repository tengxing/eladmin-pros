<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.littleterry.modules.sys.mapper.SysPermissionMapper">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="cn.littleterry.modules.sys.entity.SysPermission" id="sysPermissionMap">
        <result property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="code" column="code"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findByRoleId" parameterType="java.lang.Long" resultMap="sysPermissionMap">
        SELECT
            p.*
        FROM
            sys_permission p
        WHERE
            p.id IN (
                SELECT
                    permission_id
                FROM
                    sys_role_permission rp
                JOIN sys_role r ON rp.role_id = r.id
                WHERE
                    r.id= #{id,jdbcType=INTEGER}
            )
    </select>


</mapper>